<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <h1><% if !!session[:user_id] %>
    <%= User.find_by(User.find_by(id: session[:user_id])).username %>
   <% end %> </h1>
    <h1><% if !!session[:user_id] %>
    <%= User.find_by(User.find_by(id: session[:user_id])).balance %>
   <% end %> </h1>

  </body>
</html>
